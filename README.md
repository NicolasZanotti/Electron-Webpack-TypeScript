# Electron with Webpack TypeScript

This repos is an example of how to use Electron 30+ with Webpack and TypeScript.

The main Node.js process still compiles to CommonJS. The renderer process uses ES Modules.
